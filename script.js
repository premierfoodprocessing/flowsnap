const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.main-nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const input=document.getElementById('video-url');const message=document.getElementById('form-message');document.getElementById('paste-btn')?.addEventListener('click',async()=>{try{input.value=await navigator.clipboard.readText();message.textContent=input.value?'Link pasted.':'Clipboard was empty.'}catch{message.textContent='Your browser blocked clipboard access. Paste the link manually.'}});document.getElementById('download-form')?.addEventListener('submit',e=>{e.preventDefault();try{const url=new URL(input.value.trim());if(!['http:','https:'].includes(url.protocol))throw new Error();message.textContent='Link accepted. The authorised processing service can be connected here.'}catch{message.textContent='Please enter a complete public web link.'}});document.getElementById('year').textContent=new Date().getFullYear();
